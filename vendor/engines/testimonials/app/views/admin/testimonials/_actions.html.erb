<ul>
  <% if Admin::TestimonialsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_testimonials_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_testimonial_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::TestimonialsController.sortable? and Testimonial.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Testimonials"),
                 admin_testimonials_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Testimonials"),
                 admin_testimonials_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>